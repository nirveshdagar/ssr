# SQLite path (defaults to ../data/ssr.db relative to template_v0/)
# SSR_DB_PATH=C:/Users/google/Desktop/ssr/data/ssr.db

# Iron-session cookie secret. Must be >= 32 chars. Set in .env.local for prod.
# SSR_SESSION_SECRET=replace-this-with-a-real-32-plus-char-secret

# Node env (next sets this automatically)
# NODE_ENV=development
