# Web + core
flask==3.1.0
requests==2.32.3
python-dotenv==1.0.1
jinja2==3.1.4

# SSH / deployment (droplet provisioning + cert installs via SA SSH fallback)
paramiko==3.5.0

# Secret encryption at rest
cryptography>=42.0

# LLM provider SDK (website generation via Anthropic if configured)
anthropic>=0.42

# Browser automation — patchright is the stealth-patched Playwright fork
# used by the ServerAvatar SSL-install UI-automation path.
patchright>=1.49.0

# Tests — needed on the server so deploy.sh can run pytest as a pre-restart gate.
pytest>=8.0
